{
  "name": "media-naming-compressor",
  "version": "0.5.0",
  "private": true,
  "description": "Online batch media naming and FFmpeg compression workbench.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "express": "^5.1.0",
    "multer": "^2.0.2"
  }
}
